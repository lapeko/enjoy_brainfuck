#!/usr/bin/env bash

echo "run..."

while getopts ":f:" file; do
    echo "arg: $file"
    case ${file} in
        f)
            ~/Desktop/brainfuck/build/brainfuck "./${OPTARG}"
            ;;
        ?)
            echo "Invalid option ${file}" >&2
            exit 1;
            ;;
    esac
done